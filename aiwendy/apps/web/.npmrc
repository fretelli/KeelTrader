# Lock all dependencies to exact versions
save-exact=true

# Use package-lock.json
package-lock=true

# Strict peer dependencies
strict-peer-dependencies=false

# Engine strict (require Node >= 20.0.0)
engine-strict=true
